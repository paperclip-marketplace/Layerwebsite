# Layerwebsite

Standalone Layer marketing site — landing page, pricing page, GSAP animations, and assets.

## Setup

```bash
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000).

## Build

```bash
npm run build
npm start
```
