/** Improve card — flat composite export (call UI + coaching card) */
const base = "/assets/images/lead/how-layer-works/improve";

export const IMPROVE_CARD_ASSETS = {
  /** Full Improve visual composite (overflows media band into copy) */
  composite: `${base}/improve-composite.webp`,
} as const;
